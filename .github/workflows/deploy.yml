/////Readme.md/////


autobuild and Deploy StrawberryFi Utility token 


name: Deploy StrawberryFi Tool

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run build
        run: echo "Build complete"

      - name: Deploy message
        run: echo "🚀 StrawberryFi deployed"