####Readme.md####

Backend Core System For StrawberryFi Utility token 


const express = require('express');
const app = express();
const apiRoutes = require('./routes/api');

app.use(express.json());
app.use('/api', apiRoutes);

const PORT = 3000;

app.listen(PORT, () => {
  console.log(`🍓 StrawberryFi running on port ${PORT}`);
});